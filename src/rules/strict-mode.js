module.exports = {
    rules: {
        /**
         * STRICT MODE
         *
         * These rules relate to strict mode directives
         */
        strict: 'off', // require or disallow strict mode directives
    },
};
